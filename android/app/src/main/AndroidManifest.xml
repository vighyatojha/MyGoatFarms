<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- Required at runtime on Android 13+ (API 33+) before any
         notification — including FCM — can be shown. NotificationService
         requests this permission via flutter_local_notifications /
         firebase_messaging at first login. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Lets zonedSchedule()'d health reminders survive a phone reboot.
         flutter_local_notifications persists its own pending schedules
         and re-registers them via this receiver when it sees
         BOOT_COMPLETED — no custom native code needed on our side.
         (HealthReminderScheduler.rescheduleAllForFarm() is a second,
         belt-and-suspenders safety net: it also fully recomputes and
         re-schedules everything from Firestore on every app open, so a
         reminder still survives even if this receiver is ever delayed
         or skipped by battery optimization on a given device.) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="My Goat Farms"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Default small icon used for FCM notifications shown by the
             OS while the app is backgrounded/terminated. Falls back to
             the launcher icon; swap for a dedicated white/transparent
             notification icon (res/drawable/ic_notification) if the
             launcher icon doesn't render well in the status bar. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />

        <!-- Default channel id. Must match NotificationService's
             AndroidNotificationChannel id so FCM notification messages
             and locally-shown foreground notifications land in the same
             channel instead of Android creating an unconfigured
             "Miscellaneous" one. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="mygoatfarms_default_channel" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>